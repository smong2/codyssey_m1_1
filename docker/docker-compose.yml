services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: weatherload_app
    ports:
      - "8080:8000"
    volumes:
      # 로컬 코드 수정 사항을 컨테이너에 즉시 동기화
      - ../src:/app/src
    env_file:
      - ../src/.env
    restart: unless-stopped
